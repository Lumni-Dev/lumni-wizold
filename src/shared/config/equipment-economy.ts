import type { EquipmentSet } from "@/models/entities/item";

export const SET_PIECE_PRICE: Record<EquipmentSet, number> = {
  bronze: 20000,
  silver: 25000,
  gold: 35000,
  diamond: 50000,
  lunar: 100000,
};
