# PostgreSQL connection for scripts/migrate.mjs and the future data adapter.
# Copy to .env.local and fill in; .env.local is never committed.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=troque-aqui
PGDATABASE=wizold-prod
